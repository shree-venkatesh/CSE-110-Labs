import { render, screen, fireEvent } from "@testing-library/react";
import '@testing-library/jest-dom';
describe('Favorite StickyNote', () => {
    it('list of favorite notes are displayed when zero note is favorited', () => {

    })
})